# 项目开发步骤

## 1 新建项目

### 1.1 VSCode 方法

```bash
# 通过 uni-app 官方模板创建（Vue3 版本）
npx degit dcloudio/uni-preset-vue#vite 项目名称

# 进入项目并安装依赖
cd 项目名称
npm install
```

### 1.2 HBuilderX 方法

HBuilderX 菜单栏 → **文件** → **新建** → **项目** → 选择 `uni-app`，勾选 `Vue3` 版本，填写项目名称即可。

## 2 配置小程序 appId

在 `manifest.json` 的 `mp-weixin` 节点下配置微信小程序 AppID：

```json
"mp-weixin": {
    "appid": "wx7a804c75d979be99"
}
```

AppID 可在 [微信公众平台](https://mp.weixin.qq.com/) → 开发管理 → 开发设置 中获取。

## 3 微信开发者工具配置

![alt text](image.png)

![alt text](image-1.png)

![alt text](image-2.png)

![alt text](image-3.png)

## 4 更改配置

### 4.1 增加 mergeVirtualHostAttributes 配置

在 `manifest.json` 的 `mp-weixin` 节点下开启：

```json
"mp-weixin": {
    "appid": "wx7a804c75d979be99",
    "mergeVirtualHostAttributes": true
}
```

**作用**：允许父组件在使用自定义组件时设置的 `class`、`style` 等属性，与子组件内部根节点的属性**合并**，实现样式透传。开启后可以直接给自定义组件添加样式：

```vue
<!-- 外部 class/style 会合并到组件内部根节点 -->
<my-card class="mt20" style="position: relative;" />
```

## 5 创建 tabBar 页面

在 `pages.json` 中配置页面路径和 tabBar：

```json
{
    "pages": [
        { "path": "pages/index/index", "style": { "navigationBarTitleText": "首页" } },
        { "path": "pages/service/service", "style": { "navigationBarTitleText": "服务" } },
        { "path": "pages/me/me", "style": { "navigationBarTitleText": "我的" } }
    ],
    "tabBar": {
        "color": "#999",
        "selectedColor": "#ffce2c",
        "backgroundColor": "#fff",
        "list": [
            {
                "pagePath": "pages/index/index",
                "text": "首页",
                "iconPath": "static/common/images/home.png",
                "selectedIconPath": "static/common/images/home-active.png"
            },
            {
                "pagePath": "pages/service/service",
                "text": "服务",
                "iconPath": "static/common/images/service.png",
                "selectedIconPath": "static/common/images/service-active.png"
            },
            {
                "pagePath": "pages/me/me",
                "text": "我的",
                "iconPath": "static/common/images/me.png",
                "selectedIconPath": "static/common/images/me-active.png"
            }
        ]
    }
}
```

> 页面文件需在 `pages/` 目录下对应创建，如 `pages/index/index.vue`。

## 6 安装 uView Plus

### 6.1 下载安装

#### 方式一：VSCode 方法

通过 npm 安装（推荐）：

```bash
npm install uview-plus
```

安装后，`uview-plus` 位于 `node_modules/` 下，由 `package.json` 统一管理版本，无需手动维护 `src/uni_modules/` 目录。

#### 方式二 HBuilderX 方法

HBuilderX 打开项目 → 插件市场搜索 `uView Plus` → 点击下载安装，自动放入 `uni_modules/`。

### 6.2 使用方法

**配置 easycom 规则**（`pages.json`）：

VSCode 方法（npm 安装，路径指向 `node_modules`）：

```json
"easycom": {
    "autoscan": true,
    "custom": {
        "^u--(.*)": "uview-plus/components/u-$1/u-$1.vue",
        "^up-(.*)": "uview-plus/components/u-$1/u-$1.vue",
        "^u-([^-].*)": "uview-plus/components/u-$1/u-$1.vue"
    }
}
```

HBuilderX 方法（插件市场安装，路径指向 `uni_modules`）：

```json
"easycom": {
    "autoscan": true,
    "custom": {
        "^u--(.*)": "@/uni_modules/uview-plus/components/u-$1/u-$1.vue",
        "^up-(.*)": "@/uni_modules/uview-plus/components/u-$1/u-$1.vue",
        "^u-([^-].*)": "@/uni_modules/uview-plus/components/u-$1/u-$1.vue"
    }
}
```

> 两种方法的 easycom 规则格式相同，区别仅在于组件路径：npm 版本从 `node_modules` 解析（`uview-plus/...`），插件市场版本从 `uni_modules` 解析（`@/uni_modules/uview-plus/...`）。

**引入组件样式**（`App.vue`）：

VSCode 方法（npm 安装）：

```vue
<style lang="scss">
    @import "uview-plus/index.scss";
</style>
```

HBuilderX 方法（插件市场安装）：

```vue
<style lang="scss">
    @import "@/uni_modules/uview-plus/index.scss";
</style>
```

**引入 SCSS 变量**（`uni.scss`）：

VSCode 方法（npm 安装）：

```scss
@import 'uview-plus/theme.scss';
```

HBuilderX 方法（插件市场安装）：

```scss
@import '@/uni_modules/uview-plus/theme.scss';
```

**引入全局配置**（`main.js`）：

```js
import uviewPlus from 'uview-plus'

import { createSSRApp } from 'vue'
export function createApp() {
    const app = createSSRApp(App)
    app.use(uviewPlus)
    return { app }
}
```

> 样式需 **两处引入**，分工不同：`App.vue` 中的 `index.scss` 提供组件实际 CSS 样式；`uni.scss` 中的 `theme.scss` 提供 SCSS 变量供页面引用。

配置完成后即可直接在模板中使用组件：

```vue
<template>
    <u-button type="primary">按钮</u-button>
    <u-input placeholder="请输入" />
</template>
```

## 7 uni_modules 下的组件 安装 以及 使用方法

`uni_modules` 是 uni-app 的插件目录，存放从插件市场安装的组件，随项目源码一起提交，不能加入 `.gitignore`。

### 7.1 安装组件

#### 方式一：HBuilderX 下载

HBuilderX 打开项目 → 插件市场搜索 → 下载安装，自动放入 `uni_modules/`。

#### 方式二：VSCode 下载

通过 npm 安装（推荐，统一版本管理）：

```bash
# uni-ui 官方组件库
npm install @dcloudio/uni-ui

# uView Plus 组件库
npm install uview-plus
```

> npm 安装后，组件位于 `node_modules/` 而非 `src/uni_modules/`，需在 `pages.json` 的 `easycom.custom` 中配置 `node_modules` 路径规则（参见第 6 章 easycom 配置）。

备选方式：前往 [uni-app 插件市场](https://ext.dcloud.net.cn/) 下载 zip 包，解压后放到 `src/uni_modules/` 下同名文件夹即可。

### 7.2 使用方法

通过 `pages.json` 的 `easycom` 自动扫描注册（`autoscan: true`），符合命名规范的组件可直接在模板中使用，**无需手动 import**：

```vue
<template>
    <u-button type="primary">按钮</u-button>
    <uni-icons type="search" />
</template>
```

## 8 首页开发

首页使用自定义导航栏（`navigationStyle: "custom"`），包含状态栏占位、胶囊按钮兼容、城市定位和搜索栏。

### 8.1 页面配置

在 `pages.json` 中为首页设置自定义导航栏：

```json
{
  "path": "pages/index/index",
  "style": {
    "navigationStyle": "custom",
    "navigationBarTitleText": "宠物小程序"
  }
}
```

### 8.2 顶部导航栏

自定义导航栏需要处理两部分偏移：

```text
┌─────────────────────────┐ ▲
│  系统状态栏（时间、电量）   │ │ --status-bar-height（CSS 变量）
├─────────────────────────┤ ▼
│   ← 胶囊按钮             │ │ 导航栏区域（与胶囊对齐）
├─────────────────────────┤
│     自定义导航栏内容       │
└─────────────────────────┘
```

**模板结构：**

```vue
<template>
  <view class="container">
    <!-- 顶部导航栏 -->
    <view class="header" :style="headerStyle">
      <!-- 状态栏：电量时间位置 -->
      <view class="status-bar"></view>
      <!-- 导航栏主体 -->
      <view class="nav-content" :style="navStyle">
        <view class="location">
          <uni-icons type="location" color="#fff" size="28" />
          <text class="city">南京市</text>
        </view>
        <view class="search-box">
          <uni-search-bar
            style="width:100%"
            radius="100"
            placeholder="搜索宠物服务"
            clear-button="none"
            cancelButton="none"
          />
        </view>
      </view>
    </view>
  </view>
</template>
```

### 8.3 胶囊按钮兼容

微信小程序右上角胶囊按钮高度和位置因设备而异，通过 `uni.getMenuButtonBoundingClientRect()` 获取胶囊信息，使自定义导航栏与胶囊对齐：

```ts
import { onLoad } from '@dcloudio/uni-app'
import { ref, computed } from 'vue'

const menuButtonInfo = ref<any>(null)

onLoad(() => {
  // #ifdef MP-WEIXIN
  menuButtonInfo.value = uni.getMenuButtonBoundingClientRect()
  // #endif
  // #ifdef WEB || APP-PLUS
  menuButtonInfo.value = {
    top: '0',
    height: '44px'
  }
  // #endif
})

// 导航栏区域整体高度 = 胶囊顶部位置 + 胶囊高度 + 底部间距
const headerStyle = computed(() => {
  let style = { height: '200px' }
  // #ifdef MP-WEIXIN
  if (menuButtonInfo.value) {
    style = {
      height: `${menuButtonInfo.value.top + menuButtonInfo.value.height + 20}px`
    }
  }
  // #endif
  // #ifdef WEB || APP-PLUS
  style = { height: '90px' }
  // #endif
  return style
})

// 导航栏内容区域定位（与胶囊按钮同行）
const navStyle = computed(() => {
  let style = { top: '0', height: '44px' }
  // #ifdef MP-WEIXIN
  if (menuButtonInfo.value) {
    style = {
      top: `${menuButtonInfo.value.top}px`,
      height: `${menuButtonInfo.value.height}px`
    }
  }
  // #endif
  // #ifdef WEB || APP-PLUS
  style = { top: '20px', height: '50px' }
  // #endif
  return style
})
```

### 8.4 城市定位

点击导航栏左侧的定位图标区域，触发 `uni.getLocation()` 获取当前经纬度，再调用高德**逆地理编码**接口反查城市名，显示在导航栏上（如"南京市"）。

#### 8.4.1 页面结构

导航栏左侧为可点击的定位区域，点击后触发定位：

```vue
<view class="location" @click="startLocation">
  <uni-icons type="location" color="#fff" size="28" />
  <text class="city">{{ cityName }}</text>
</view>
```

`cityName` 为响应式变量，默认空字符串，定位成功后显示城市名。

#### 8.4.2 定位逻辑

```ts
const cityName = ref<string>('')

const startLocation = () => {
  uni.getLocation({
    type: 'wgs84',
    success: (res) => {
      reverseCode(res.latitude, res.longitude).then((res) => {
        cityName.value = res            // 显示城市名
      }).catch(() => {
        cityName.value = '无法定位'
      })
    },
    fail: (err) => {
      cityName.value = '无法获取位置'
      // ...分平台权限引导，见 8.4.4
    }
  })
}
```

流程：`uni.getLocation()` 拿到经纬度 → `reverseCode()` 高德逆地理编码 → 城市名回填到 `cityName`。

#### 8.4.3 逆地理编码工具（utils/geoCode.ts）

封装高德 Web 服务 API（`/v3/geocode/regeo`），把经纬度转为城市名：

```ts
export const reverseCode = (latitude: number, longitude: number): Promise<string> => {
  return new Promise((resolve, reject) => {
    // 参数校验：非数字直接 reject
    uni.request({
      url: 'https://restapi.amap.com/v3/geocode/regeo',
      data: {
        key: '高德Web服务Key',           // 需在高德开放平台申请
        location: `${longitude},${latitude}`, // 注意顺序：经度,纬度
      },
      success(res) {
        const data = res.data
        if (data.status !== '1') return reject('定位失败')
        const { city, province } = data.regeocode.addressComponent
        resolve(city.length ? city : province)  // 直辖市 city 为空，用 province 兜底
      },
      fail(err) {
        reject(err)
      },
    })
  })
}
```

要点：

- **参数顺序**：`location` 必须是 `经度,纬度`，反了会导致定位失败
- **直辖市兜底**：北京、上海等直辖市 `addressComponent.city` 为空，需用 `province` 替代
- **Key 类型**：需使用高德 **Web 服务** 类型的 key（不同于 JS API key）

#### 8.4.4 定位失败与权限引导（分平台）

定位失败时弹出提示框，用户确认后按平台跳转开启定位权限：

| 平台 | 处理方式 |
| ---- | ---- |
| 微信小程序 | `uni.openSetting()` 打开小程序设置页，用户手动开启定位权限 |
| App | 提示前往**系统设置**开启定位权限 |
| H5 | 提示前往**浏览器设置**开启定位权限 |

### 8.5 banner图

### 涉及的关键知识点

| 知识点 | 说明 |
| ------ | ---- |
| `navigationStyle: "custom"` | 隐藏原生导航栏，自定义实现 |
| `--status-bar-height` | uni-app 提供的 CSS 变量，值为设备状态栏高度 |
| `uni.getMenuButtonBoundingClientRect()` | 获取微信胶囊按钮位置/尺寸，仅微信小程序可用 |
| `#ifdef` / `#endif` | 条件编译，不同平台执行不同逻辑 |
| `computed` | 响应式计算，根据胶囊信息动态调整导航栏高度 |
